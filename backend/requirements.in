# Runtime dependencies: the allowed ranges. The exact, hash-pinned versions that Docker and CI
# install are in requirements.txt, generated from this file (see the command there).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
sqlalchemy>=2.0.30,<2.1
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0
# Password hashing (Argon2id) and access tokens (JWT, HS256).
argon2-cffi>=23.1,<26.0
PyJWT>=2.8,<3.0
# IANA time zones for log sources (zoneinfo), independent of the OS having them installed.
tzdata>=2024.1
